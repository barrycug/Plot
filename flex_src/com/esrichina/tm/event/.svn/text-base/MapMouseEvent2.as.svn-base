package com.esrichina.tm.event
{
	import com.esri.ags.Graphic;
	import com.esri.ags.geometry.MapPoint;
	
	import flash.events.MouseEvent;

	public class MapMouseEvent2 extends MouseEvent
	{
		
		public function MapMouseEvent2(type:String,graphic:Graphic,mapPoint:MapPoint)
		{
			
			super(type, true, false, localX, localY, relatedObject, ctrlKey, altKey, shiftKey, buttonDown, delta);
		}
		
	}
}